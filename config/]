const mongoose = require("mongoose")

const config = require("config")

const connect = async () => {
  try {

    mongoose.connect(config.get('mongoDB_URI'))
  }
  catch (error) {
    console.error(error)
  }
}





